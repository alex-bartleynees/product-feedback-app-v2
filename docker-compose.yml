version: '3'
services:
  nodejs:
    build: ./
    image: suggestions.azurecr.io/product-feedback-app
    container_name: product-feedback-app
  nginx:
    build: ./nginx
    image: suggestions.azurecr.io/tiotobing/nginx
    container_name: nginx
    ports:
      - 8080:80

// import { ProviderToken, inject } from '@angular/core';
// import { signalStoreFeature, withMethods } from '@ngrx/signals';

// export const withService = <T>(service: ProviderToken<T>) =>
//   signalStoreFeature(
//     withMethods((state) => {
//       const s = inject(service);
//       return {
//         service: s,
//       };
//     })
//   );
